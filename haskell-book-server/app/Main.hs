-- Главный модуль веб-сервера для книги по Haskell
-- module - ключевое слово для объявления модуля
-- Main - имя модуля, должно совпадать с именем файла
-- where - ключевое слово, после которого идет содержимое модуля
module Main where

-- Определяем типы-синонимы для лучшей читаемости кода
-- type - ключевое слово для создания синонима существующего типа
-- UserId = Int означает, что UserId это просто другое имя для Int
type UserId = Int      -- Идентификатор пользователя
type ChapterId = Int   -- Идентификатор главы
type Title = String    -- Заголовок главы (String это список символов [Char])
type Content = String  -- Содержимое главы

-- Функция для получения главы по её идентификатору
-- :: - оператор объявления типа функции
-- ChapterId -> IO (Maybe Content) читается как:
-- "функция принимает ChapterId и возвращает IO действие с Maybe Content"
-- IO - тип для операций ввода-вывода (побочные эффекты)
-- Maybe - тип который может содержать значение (Just value) или ничего (Nothing)
getChapter :: ChapterId -> IO (Maybe Content)
getChapter chapterId = do
    -- do - синтаксический сахар для монадических вычислений
    -- <- используется для извлечения значения из монады
    -- return - функция для "упаковки" значения в монаду
    -- $ - оператор применения функции (избегает скобок)
    -- Just - конструктор типа Maybe, означает "есть значение"
    return $ Just "Chapter content" 

-- Главная функция программы - точка входа
-- main всегда имеет тип IO ()
-- () - unit type, означает "нет полезного возвращаемого значения"
main :: IO ()
main = do
    -- putStrLn - функция для вывода строки с переводом строки
    -- :: String -> IO ()
    -- Выводим приветственные сообщения
    putStrLn "🚀 Запускаем веб-сервер..."
    putStrLn "Сервер доступен на http://localhost:8080"
    putStrLn "Hello, Web Server!"

